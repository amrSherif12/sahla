import 'package:flutter/material.dart';

class Undefined extends StatelessWidget {
  const Undefined({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('undefined screen'),
      ),
    );
  }
}
